import React, { FC } from 'react';
import './create-contexts.css';

interface CreateContextsProps {}

const CreateContexts: FC<CreateContextsProps> = () => (
  <div className="create-contexts" data-testid="CreateContexts">
    CreateContexts Component
  </div>
);

export default CreateContexts;
